/**
 * Logs a message to the console and returns true.
 * @param str - The string to log.
 * @returns True.
 */
export function bar(str: string): boolean {
  console.log("bar" + " " + str);

  return true;
}
